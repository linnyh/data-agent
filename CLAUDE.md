## Agent skills

### Issue tracker

Issue 与 spec 存放在 GitHub Issues（`gh` CLI，仓库 `linnyh/data-agent`）。See `docs/agents/issue-tracker.md`。

### Triage labels

五个标准 triage 角色，标签字符串与角色名一致。See `docs/agents/triage-labels.md`。

### Domain docs

单上下文布局：根目录 `CONTEXT.md` + `docs/adr/`。See `docs/agents/domain.md`。
