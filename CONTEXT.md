# 数枢（DataPivot）

数枢是一个对话式数据分析 Agent：用户上传数据并与 Agent 多轮对话，Agent 自动完成分析并给出叙述解读与结构化结果。由早期数据分析管线以 LangGraph 重构而来。

## Language

### 对话与交互

**会话**:
用户与 Agent 之间的一段多轮对话，绑定一份数据集，从创建到结束拥有唯一标识。
_Avoid_: thread（LangGraph 实现术语）、聊天、任务

**会话创建**:
开启新会话的两种途径：**显式创建**（点击"新建会话"按钮）与**隐式创建**（在首页发送首条分析目标或上传文件时由系统自动创建）。
_Avoid_: auto-create、自动建会

**分析目标**:
用户在一个会话中提出的具体数据分析问题（如"哪些基金符合条件"）。
_Avoid_: question、需求、任务

**澄清**:
Agent 在执行分析前，因分析目标存在歧义（口径、范围、输出列）而向用户发起的提问。

**追问**:
用户在看到结果后，基于同一数据集发起的后续分析（如"换个口径重算"）。

**会话历史**:
一个会话中历次分析目标与其结果的按序记录，供界面回放与状态恢复。
_Avoid_: 消息记录、聊天记录、对话日志

**结果**:
一次分析执行交付给用户的产出，由三部分组成：叙述解读（自然语言结论）、结构化表格（可下载 CSV）与可选图表（可下载 PNG）。
_Avoid_: 输出、答案、prediction

**图表**:
结果中的可视化部分，由图表规格描述，前端渲染为可交互图像（悬停值、图例）并可导出 PNG。Agent 在解读结果时自主判断是否附图；用户显式要求画图时必画。
_Avoid_: 图、plot、figure

**图表规格**:
对一张图表的声明式描述（图表类型、类目轴、数据系列），由 Agent 生成，须与结果表格数据对齐。
_Avoid_: chart config、绘图代码、echarts option（实现术语）

### 数据

**数据集**:
用户上传给一个会话的数据文件集合（结构化数据、文档、可选视频简报），保存在该会话专属目录。
_Avoid_: 上下文、context（历史遗留词）、文件包

**数据源**:
数据集中的结构化表格文件（csv / json / sqlite），注册后可被统一 SQL 查询。
_Avoid_: 表、table（泛指时）

**文档**:
数据集中的散文式文件（md / pdf），需经结构化抽取才能被分析使用。
_Avoid_: doc 原文、长文

**视频简报**:
数据集中可选的操作讲解视频（briefing.mp4），承载配置面板、筛选阈值、统计口径等权威规则。
_Avoid_: 视频、video（泛指时）

### 执行

**执行管线**:
一次分析执行的自动化链条：视频预处理 → 文档相关性筛选 → 文档抽取 → 表筛选 → 求解。管线内部全自动，不打断。
_Avoid_: pipeline、流程、流水线

**求解**:
执行管线的核心环节：Agent 编写并运行求解代码，对数据源做 SQL 分析，产出结果表格。

**尝试**:
求解的一个执行单元；一次求解失败后可恢复初始状态重试，全部尝试失败才走兜底。
_Avoid_: attempt（实现术语）、重试轮次
